ADD_EXECUTABLE (model1contact1 ${PROJECT_SOURCE_DIR}/testSource/model1contact1/main.cpp)
IF (USE_BULLET)
   TARGET_LINK_LIBRARIES(model1contact1 ${BULLET_LIBRARIES} OMD)
ELSE (USE_BULLET)
   TARGET_LINK_LIBRARIES(model1contact1 OMD)
ENDIF (USE_BULLET)

